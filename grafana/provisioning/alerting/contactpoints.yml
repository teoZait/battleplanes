apiVersion: 1
contactPoints:
  - orgId: 1
    name: slack
    receivers:
      - uid: slack-webhook
        type: slack
        settings:
          url: $SLACK_WEBHOOK_URL
          title: |
            {{ if eq .Status "firing" }}ALERT{{ else }}RESOLVED{{ end }}: {{ .CommonLabels.alertname }}
          text: |
            {{ range .Alerts }}
            {{ .Annotations.summary }}
            {{ end }}
